string name = 'tugba';
string lastName ='Salis';
string fullName = name + ' ' + lastName;
system.debug(fullName);

string justName = fullName.remove('tugba');
system.debug(justName);

string up = fullName.toUpperCase();
system.debug(up);
Integer kalk = fullName.length();
system.debug(kalk);
